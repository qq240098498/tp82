# tp82 依赖台账与版本核查平台

一个本地小工具：把各个项目用到的依赖集中登记起来。页面上先建项目，再往项目下面登记依赖名称、版本、许可、责任人与状态；同一个依赖被两个项目用到时，版本高低一眼能比出来。依赖名写得不合规矩、版本不是三段数字、同一个项目里重复登记同一个依赖，保存时当场就会指出来。版本对照区按依赖名汇总散在各项目下的登记：用了几个项目、各是什么版本、哪些不一致；不一致的可以下钻到具体项目与条目，选一个目标版本先预演改动清单，确认后统一执行，并能回看这次改了哪些项目、从什么版本改成什么版本。

数据全部存在本机的数据文件里，不连数据库，也不调用任何外部服务。

## 技术栈

- 后端：Node.js（>=18）与 Express，前后端同一个进程
- 前端：原生 HTML、CSS 与 JavaScript，页面文件由后端静态目录直接提供，没有构建步骤
- 存储：本地 JSON 文件 `data/db.json`

## 项目结构

```
tp82/
├── server/
│   ├── index.js        # 服务入口、静态目录、接口路由
│   ├── api.js          # 项目、依赖登记与版本对照的对外动作
│   ├── projects.js     # 项目的校验、新增、修改与删除
│   ├── deps.js         # 依赖登记的校验、新增、修改、查询与删除
│   ├── versions.js     # 按依赖汇总、版本高低比较、统一预演与执行
│   ├── errors.js       # 带错误码与出错位置的业务异常
│   └── store.js        # 数据读写：载入、整理结构与落盘
├── public/
│   ├── index.html      # 主界面：版本对照、项目区、依赖登记区与登记表单
│   ├── app.js          # 页面交互逻辑
│   └── style.css       # 页面样式
├── data/
│   └── db.json         # 本地数据文件（含三个项目与十九条依赖登记）
├── package.json
├── .gitignore
└── README.md
```

## 启动方式

```bash
npm install
npm start
```

启动后打开页面地址：<http://localhost:5082>

端口固定为 `5082`，如需临时改动可在启动前设置环境变量 `PORT`。

## 页面怎么用

1. 顶栏右侧可以填当前操作者，填过之后浏览器会记住，新建登记时会把它带进责任人
2. 项目区列出项目名称、负责人、说明与这个项目下已经登记了多少条依赖；可以新增项目，也可以改名、换负责人、删除
3. 依赖登记区顶部可以按项目、状态、许可筛选，再按依赖名、责任人或者备注搜索，右上角刷新按钮重新拉取一次
4. 点新建登记，选好所属项目，填上依赖名称、版本，按需要补许可、责任人、状态与备注；列表里点编辑会把这条登记回填到表单
5. 任何一步不成立时，页面顶部给出说明，并尽量把问题标到对应的输入项上；删除项目与删除登记都会先确认

项目下面还挂着依赖登记时不允许删除项目，页面上会写清还剩多少条、例如哪几条，先处理这些登记再删。

## 版本对照与统一

1. 版本对照区按依赖名汇总：这个依赖一共被几个项目用过、有哪几个版本、各有多少项目在用，版本之间是否不一致；默认只列不一致的，也可以按依赖名搜索
2. 点下钻可以看到具体是哪些项目的哪些登记条目（版本、许可、责任人、状态、备注都在），以及版本从低到高的排列，相邻版本差在主版本号、次版本号还是修订号上分别写清楚
3. 建议统一版本默认标星：依据是“被最多项目使用”的那个版本，改动面最小、最稳妥；得票并列时取并列版本里版本号更高的
4. 在明细里选好目标版本后点预演：页面列出哪几条登记要改、属于哪个项目、从什么版本改成什么版本、哪些保持不动；预演只计算不落盘
5. 确认执行后才会真正修改登记（只改版本号，其他字段原样保留），结果页列出本次改动的项目与版本变化，背景的登记表和对照汇总同步刷新
6. 目标版本只能选已经有项目在用的版本；全部登记已经是目标版本时不会产生改动

## 登记口径

- 依赖名称以小写字母或数字开头，可以用小写字母、数字、点、下划线、短横线与斜线，最长 60 个字符；带范围的写法例如 `@types/node` 也按这个口径登记
- 版本写成三段数字，例如 `2.7.18`，需要时可以带一段预发布后缀，例如 `5.0.10-rc.1`
- 许可按实际名称填写，例如 `MIT`、`Apache-2.0`、`BSD-3-Clause`；还没确认的可以留空，列表里会显示成未填
- 责任人可以留空，表示这条依赖还没有定人
- 状态只有三种：在用、待升、已弃用
- 备注用来写为什么用这个版本、有没有待办，最长 200 个字符
- 同一个项目下不允许出现同名的依赖，同一个依赖在不同项目里各登记各的、互不覆盖

## 数据存储

所有数据写在 `data/db.json`，结构为 `{ "projects": [...], "deps": [...] }`。落盘时先把内容写进同名临时文件，再用改名的方式替换正式文件，写入过程中断也不会把数据文件写坏；文件缺失或内容损坏时会自动回落到初始数据。

## 接口一览

| 方法   | 路径                | 说明                                             |
| ------ | ------------------- | ------------------------------------------------ |
| GET    | /api/health         | 服务状态检查                                     |
| GET    | /api/projects       | 项目清单，附带每个项目的依赖条数                 |
| POST   | /api/projects       | 新增项目                                         |
| PATCH  | /api/projects/:id   | 修改项目的名称、负责人或说明                     |
| DELETE | /api/projects/:id   | 删除项目                                         |
| GET    | /api/deps           | 依赖登记列表，支持按项目、状态、许可筛选与关键词搜索 |
| POST   | /api/deps           | 新增依赖登记                                     |
| GET    | /api/deps/:id       | 查看单条依赖登记                                 |
| PATCH  | /api/deps/:id       | 修改依赖登记                                     |
| DELETE | /api/deps/:id       | 删除依赖登记                                     |
| GET    | /api/versions       | 按依赖名汇总：项目数、版本分布、一致性与推荐版本（`onlyInconsistent`、`keyword` 可选） |
| GET    | /api/versions/detail?name=... | 某条依赖的下钻明细：具体项目、登记条目与版本高低关系 |
| POST   | /api/versions/unify/preview | 统一版本预演，只返回改动清单不落盘               |
| POST   | /api/versions/unify | 执行统一，把目标版本之外的登记批量改成目标版本   |

接口出错时统一返回 `{ "error": { "code": "...", "message": "...", "field": "..." } }`，其中 `field` 用于让页面把问题标到具体的输入项上。
